{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "copy-markdown-link-for-notion",
  "title": "Copy Markdown Link for Notion",
  "description": "Copy the current Notion page as a Markdown link in the format `[title](url)`. ※This extension only works with the Notion Desktop app / この拡張機能はNotionのデスクトップアプリでのみ動作します。",
  "icon": "extension-icon.png",
  "author": "atsushi_kawamura",
  "owner": "a2c",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "get-notion-page-as-markdown-link",
      "title": "Get Notion Page as Markdown Link",
      "description": "Works only with Notion Desktop app / Notionのデスクトップアプリのみ対応",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.5",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
